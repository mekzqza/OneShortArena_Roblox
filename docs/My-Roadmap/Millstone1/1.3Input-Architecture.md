🏗️ Software Architecture Overview
โปรเจกต์นี้ใช้สถาปัตยกรรมแบบ Event-Driven Architecture ร่วมกับ Centralized Networking เพื่อเน้นความปลอดภัย (Security), การแยกส่วนการทำงาน (Decoupling), และความง่ายในการขยายระบบ (Scalability)

1. Core Networking System (ระบบเครือข่าย)
ใช้รูปแบบ Single Remote Event Pattern เพื่อจัดการการสื่อสารระหว่าง Client และ Server ผ่านท่อส่งข้อมูลเดียว ลดความซับซ้อนของการจัดการ RemoteEvent จำนวนมาก

Network Bridge: ใช้ RemoteEvent เพียงตัวเดียว (SystemsShared/Network/NetworkBridge) ในการรับ-ส่งข้อมูลทั้งหมด

Network Handler (Server): ตัวกลางจัดการคำขอจาก Client ทำหน้าที่เป็น Gatekeeper คอยตรวจสอบความปลอดภัยก่อนส่งต่อให้ Game Logic

Network Controller (Client): ตัวกลางจัดการการส่งข้อมูลจากฝั่งผู้เล่น โดยทำงานร่วมกับ EventBus เพื่อให้ Controller อื่นๆ ไม่ต้องยุ่งกับ RemoteEvent โดยตรง

2. Event-Driven Communication (การสื่อสารภายใน)
ใช้รูปแบบ Publisher-Subscriber (Pub/Sub) ผ่าน EventBus เพื่อลดการผูกติดกันของโค้ด (Decoupling)

EventBus: ระบบไปรษณีย์กลางสำหรับกระจายข่าวสารภายในระบบ (ทั้งฝั่ง Client และ Server แยกกัน)

Decoupled Logic: ระบบต่างๆ (เช่น Combat, UI, Data) ไม่เรียกใช้ฟังก์ชันข้ามกันโดยตรง แต่จะสื่อสารผ่านการ "ประกาศ Event" (Emit) และ "รอฟัง Event" (On) แทน

Network Integration: การสั่งงานข้าม Server/Client ทำผ่าน Event พิเศษ (NETWORK_SEND) ทำให้ Logic เกมไม่ต้องรับรู้เรื่อง Networking Layer

3. Input System (ระบบควบคุม)
ใช้รูปแบบ Action-Based Input เพื่อแยกปุ่มกดออกจากการกระทำ (Separation of Controls from Actions)

Centralized Input Controller: จุดรับ Input เพียงจุดเดียวที่จัดการ ContextActionService

Action Mapping: แปลง "ปุ่มกด" (Physical Input เช่น Key F, Touch Tap) ให้เป็น "คำสั่ง" (Semantic Action เช่น CAST_SKILL)

Cross-Platform Support: รองรับ PC, Console, และ Mobile โดยอัตโนมัติผ่านการ map action เดียวกัน

4. Security & Validation (ความปลอดภัย)
เน้นความปลอดภัยแบบ Server-Authoritative และ Defense in Depth

Allowlist System: อนุญาตเฉพาะ Event ที่ลงทะเบียนไว้เท่านั้นให้ผ่านเข้า-ออก Server ได้

Payload Guard: ตรวจสอบชนิดข้อมูล (Type Checking) และขนาดข้อมูล (Size Limit) ป้องกันการส่ง Table ขนาดใหญ่หรือ Instance ที่อันตราย

Rate Limiting: จำกัดจำนวนคำขอต่อวินาทีของผู้เล่นแต่ละคน เพื่อป้องกันการ Spam หรือ DoS Attack

Logic Validators: ระบบตรวจสอบความถูกต้องของข้อมูล (เช่น เช็ค Cooldown, เช็คสถานะ) ก่อนเริ่มทำงานจริง

ตัวอย่างแผนภาพ (ถ้าต้องการใส่ใน Docs)
Architecture Diagram:

Client Logic (Emit) ➡️ EventBus ➡️ NetworkController ➡️ NetworkBridge ➡️ NetworkHandler ➡️ EventBus ➡️ Server Logic